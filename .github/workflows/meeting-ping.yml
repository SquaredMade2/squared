name: "Send meeting notifications"

on:
  schedule:
    - cron: "0 19 * * 0" 
    - cron: "0 19 * * 4"
    - cron: "0 19 1-7,15-21 * 2" 
  workflow_dispatch:

jobs:
  send-notifications:
    runs-on: ubuntu-latest
    steps:
      - name: Send meeting notification to Discord
        env:
          DISCORD_MEETINGS_URL: ${{ secrets.DISCORD_MEETINGS_URL }}
        run: |
          # Check if today is Tuesday (to ping @everyone)
          DAY_OF_WEEK=$(date +%u)

          MESSAGE="There is a meeting scheduled."
          if [ "$DAY_OF_WEEK" -eq 2 ]; then
            MESSAGE="@everyone There is a meeting scheduled today!"
          fi

          # Send the notification to Discord
          curl -H "Content-Type: application/json" -X POST \
            -d "{\"content\": \"$MESSAGE\"}" \
            "${DISCORD_MEETINGS_URL}"
