
npx commitlint --edit $1

# Define the desired author information
DESIRED_AUTHOR_NAME="yoloOnTheBattlefield"
DESIRED_AUTHOR_EMAIL="cristianfloreadev@gmail.com"

# Set the author information locally
git config user.name "$DESIRED_AUTHOR_NAME"
git config user.email "$DESIRED_AUTHOR_EMAIL"

echo "Set author information locally to $DESIRED_AUTHOR_NAME <$DESIRED_AUTHOR_EMAIL>."

echo "Running Formatting..."

pnpm format:write
git add .

# Allow the commit to proceed
exit 0