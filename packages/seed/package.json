{
  "name": "@repo/seed",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "db:seed": "cross-env NODE_ENV=test ts-node src/index.ts",
    "test:db:seed": "cross-env NODE_ENV=test ts-node src/seed-test-db.ts",
    "docker:db": "docker-compose -f docker-compose.yml up -d",
    "docker:db:down": "docker-compose -f docker-compose.yml down --volumes"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "dotenv": "^16.4.1"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.0.0",
    "@repo/typescript-config": "workspace:*",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.4.1",
    "bcryptjs": "^2.4.3",
    "cross-env": "^7.0.3",
    "ts-node": "^10.9.2",
    "tslib": "^2.6.3"
  }
}