{
  "name": "@squared/fonts",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src/fonts",
    "tailwind-plugin.js"
  ],
  "scripts": {
    "build": "tsc && npm run build:css",
    "build:css": "mkdir -p dist && cp src/styles.css dist/styles.css && cp -R src/fonts dist/fonts",
    "postinstall": "npm run build"
  },
  "keywords": [
    "font",
    "noirden-sans",
    "tailwindcss"
  ],
  "author": "squared",
  "license": "MIT",
  "peerDependencies": {
    "tailwindcss": "^3.0.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22.7.5",
    "tailwindcss": "^3.0.0",
    "typescript": "^4.5.0"
  }
}