{
  "name": "@repo/test-db",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "prisma generate",
    "db:generate": "prisma generate && pnpm run db:test:generate",
    "db:push": "prisma db push --skip-generate && pnpm run db:test:push",
    "db:seed": "cross-env NODE_ENV=test ts-node src/seed.ts",
    "docker:db": "docker-compose -f docker-compose.yml up -d",
    "docker:db:down": "docker-compose -f docker-compose.yml down --volumes",
    "postinstall": "pnpm run db:generate",
    "studio": "prisma studio",
    "reset": "pnpm prisma migrate reset --force --schema prisma/testing-schema.prisma",
    "test:studio": "prisma studio --schema prisma/testing-schema.prisma",
    "db:test:generate": "prisma generate --schema prisma/testing-schema.prisma",
    "db:test:push": "prisma db push --skip-generate --schema prisma/testing-schema.prisma",
    "db:test:create:schema": "ts-node src/generate-testing-schema.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.0.0",
    "@repo/typescript-config": "workspace:*",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.4.1",
    "bcryptjs": "^2.4.3",
    "cross-env": "^7.0.3",
    "prisma": "^5.18.0",
    "ts-node": "^10.9.2",
    "tslib": "^2.6.3"
  }
}