{
	"name": "@squaredmade/cron-jobs",
	"version": "1.0.0",
	"publishConfig": {
		"access": "public"
	},
	"description": "CRON job framework for Squared",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "bun ./build.ts",
		"clean": "rimraf dist && rimraf node_modules",
		"check-types": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"@squaredmade/logger": "1.1.2",
		"cron": "^4.3.0"
	},
	"devDependencies": {
		"@squaredmade/builder": "^1.0.2",
		"@squaredmade/typescript-config": "^1.0.0",
		"@types/cron": "^2.0.1",
		"@types/node": "^20.10.5",
		"ts-node": "^10.9.2",
		"typescript": "^5.5.4",
		"vitest": "^3.1.3"
	}
}
